{
  "title": "Dfns: your own functions {⍺ ⍵}",
  "blocks": [
    {
      "type": "prose",
      "md": "A dfn is a direct function written in braces. Inside the braces, `⍵` is the right argument and `⍺` is the left argument. The last executed expression is the result."
    },
    {
      "type": "cell",
      "code": "{⍵ × ⍵} 9"
    },
    {
      "type": "prose",
      "md": "A dfn that uses only `⍵` is monadic. A dfn that uses `⍺` and `⍵` is dyadic. Assign it with `←`, then call it like any other function."
    },
    {
      "type": "cell",
      "code": "dbl ← {2 × ⍵} ⋄ dbl 1 2 3 4"
    },
    {
      "type": "cell",
      "code": "mean ← {(+/⍵) ÷ ≢⍵} ⋄ mean 2 4 9 5"
    },
    {
      "type": "prose",
      "md": "The argument can be a whole array. If the body uses scalar functions, they apply across that array. `dbl` doubled every item without a loop because `×` already knows how to act on arrays."
    },
    {
      "type": "exercise",
      "id": "dfns-square",
      "prompt": "Define `sq`, a function that squares its argument. It will be tested on 12 and should give 144.",
      "solution": "sq ← {⍵ × ⍵}",
      "test": "sq 12",
      "inputs": [],
      "expected": "144",
      "hints": [
        "Start from the value or array named in the prompt.",
        "inside the dfn, multiply ⍵ by itself"
      ],
      "explain": "The key move is: inside the dfn, multiply ⍵ by itself. The expression then produces the requested result directly."
    },
    {
      "type": "exercise",
      "id": "dfns-mean",
      "prompt": "Define `mean` as sum divided by count. It will be tested on `10 20 30 40` and should give 25.",
      "solution": "mean ← {(+/⍵) ÷ ≢⍵}",
      "test": "mean 10 20 30 40",
      "inputs": [],
      "expected": "25",
      "hints": [
        "Inside the dfn, ⍵ is the whole vector.",
        "Compute +/⍵ and divide by ≢⍵."
      ],
      "explain": "+/⍵ sums all values and ≢⍵ counts them. Dividing the sum by the count gives the mean."
    },
    {
      "type": "exercise",
      "id": "dfns-dyadic",
      "prompt": "Define dyadic `pct`, where `A pct B` returns `A` as a percentage of `B`. It will be tested as `30 pct 200` and should give 15.",
      "solution": "pct ← {100 × ⍺ ÷ ⍵}",
      "test": "30 pct 200",
      "inputs": [],
      "expected": "15",
      "hints": [
        "Start from the value or array named in the prompt.",
        "use ⍺ for the left argument and ⍵ for the right argument"
      ],
      "explain": "The key move is: use ⍺ for the left argument and ⍵ for the right argument. The expression then produces the requested result directly."
    },
    {
      "type": "exercise",
      "id": "dfns-range-sum",
      "prompt": "Define a function that sums the first `⍵` integers. It will be tested on 10 and should give 55.",
      "solution": "tri ← {+/⍳⍵}",
      "test": "tri 10",
      "inputs": [],
      "expected": "55",
      "hints": [
        "The argument is the upper bound.",
        "Make ⍳⍵, then reduce it with +/."
      ],
      "explain": "⍳⍵ builds the first ⍵ integers. +/ sums that range into the triangular number."
    },
    {
      "type": "exercise",
      "id": "dfns-evens",
      "prompt": "Define `evens`, a function that keeps only the even items of its argument. It will be tested on `⍳10`.",
      "solution": "evens ← {(0=2|⍵)/⍵}",
      "test": "evens ⍳10",
      "inputs": [],
      "expected": "2 4 6 8 10",
      "hints": [
        "Build a Boolean mask from ⍵.",
        "0=2|⍵ marks even items; use that mask to compress ⍵."
      ],
      "explain": "The residue test creates a 1 exactly where an item is even. Compressing ⍵ by that mask keeps only those items."
    }
  ]
}
